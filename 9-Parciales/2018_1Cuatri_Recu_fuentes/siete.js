function mostrar()
{
	var alumnos=0;
	var notas;
	var notaIngresasda;
	var sexo;
	var acumuladorDeNota=0;
	var notasContador=0;
	var sexoF=0;
	var sexoM=0;
	var promedio;
	var mensaje;
	var sexoNotaMasBaja


/*
	while(alumnos < 5)
	{
		notas= prompt("ingrese la nota del alumno");
		notas=parseInt(notas);
		if(notas >=0 || notas <=10)
		{
			acumuladorDeNota= notas + acumuladorDeNota
			notasContador = notasContador + 1;
		}
		sexo=prompt("ingrese el sexo de alumno");
		
		if(sexo == "f")
		{
			sexoF = sexoF + 1;
		}else{
			if(sexoM == "m"){
				sexoM = sexoM + 1 
			}//if(sexoM=="m")
		}//if(sexo=="f")
		if()
		alumnos = alumnos + 1;
		promedio= acumuladorDeNota / alumnos;
		mensaje="el promedio es " + promedio;
	}
	alert(mensaje);*/
}

